<UserControl x:Class="NIM.UIManagement.Preview.PreviewShellServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:localization="clr-namespace:NIM.UIManagement.Localization"
             AutomationProperties.Name="{localization:PreviewMessage ShellServices_Title}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/PreviewControlStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibility" />
            <Style x:Key="ShellServicesTab" TargetType="TabItem">
                <Setter Property="Padding" Value="14,8" />
                <Setter Property="Foreground" Value="{StaticResource PreviewBrushTextSecondary}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0,0,0,2" />
                <Setter Property="FocusVisualStyle" Value="{StaticResource PreviewFocusVisual}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="Chrome" Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Chrome" Property="Background" Value="{StaticResource PreviewBrushSurfaceInteractive}" />
                                    <Setter Property="Foreground" Value="{StaticResource PreviewBrushTextPrimary}" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource PreviewBrushTextPrimary}" />
                                    <Setter Property="BorderBrush" Value="{StaticResource PreviewBrushAccentGold}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="{StaticResource PreviewBrushSurfacePrimary}"
            BorderBrush="{StaticResource PreviewBrushBorder}" BorderThickness="1"
            CornerRadius="{StaticResource PreviewRadiusLarge}">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Margin="0,0,0,16">
                <Button x:Name="CloseButton" DockPanel.Dock="Right" Padding="12,5"
                        Style="{StaticResource PreviewButtonSecondary}" Click="CloseButton_Click"
                        Content="{localization:PreviewMessage Common_Action_Close}" />
                <StackPanel>
                    <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource PreviewBrushTextPrimary}"
                               Text="{localization:PreviewMessage ShellServices_Title}" />
                    <TextBlock Margin="0,5,0,0" Foreground="{StaticResource PreviewBrushTextMuted}"
                               TextWrapping="Wrap" Text="{localization:PreviewMessage ShellServices_Description}" />
                </StackPanel>
            </DockPanel>
            <TabControl x:Name="ServicesTabs" Grid.Row="1" Background="Transparent" BorderThickness="0">
                <TabItem Style="{StaticResource ShellServicesTab}" Header="{localization:PreviewMessage ShellServices_About_Tab}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,16,8,0">
                            <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="{StaticResource PreviewBrushTextPrimary}"
                                       Text="{localization:PreviewMessage Shell_Product_Name}" />
                            <TextBlock Margin="0,4,0,18" Foreground="{StaticResource PreviewBrushAccentGold}"
                                       Text="{Binding ProductVersion, StringFormat=Version {0}}" />
                            <ItemsControl ItemsSource="{Binding Components}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,7" Padding="10" Background="{StaticResource PreviewBrushSurfaceInteractive}"
                                                BorderBrush="{StaticResource PreviewBrushBorder}" BorderThickness="1" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="160" /><ColumnDefinition Width="140" />
                                                    <ColumnDefinition Width="120" /><ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Foreground="{StaticResource PreviewBrushTextPrimary}" Text="{Binding Component}" />
                                                <TextBlock Grid.Column="1" Foreground="{StaticResource PreviewBrushTextMuted}" Text="{Binding AssemblyName}" />
                                                <TextBlock Grid.Column="2" Foreground="{StaticResource PreviewBrushActionBlue}" Text="{Binding Version}" />
                                                <TextBlock Grid.Column="3" Foreground="{StaticResource PreviewBrushTextSecondary}" Text="{Binding Status}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
                <TabItem Style="{StaticResource ShellServicesTab}" Header="{localization:PreviewMessage ShellServices_Diagnostics_Tab}">
                    <Grid Margin="0,16,0,0">
                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                        <WrapPanel Margin="0,0,0,10">
                            <Button Padding="12,5" Style="{StaticResource PreviewButtonPrimary}"
                                    Command="{Binding ExportDiagnosticsCommand}"
                                    Content="{localization:PreviewMessage ShellServices_Diagnostics_ExportAction}" />
                            <Button Margin="8,0,0,0" Padding="12,5" Style="{StaticResource PreviewButtonSecondary}"
                                    Command="{Binding RefreshCommand}"
                                    Content="{localization:PreviewMessage Common_Action_Refresh}" />
                        </WrapPanel>
                        <ListBox Grid.Row="1" ItemsSource="{Binding Entries}" Background="Transparent"
                                 BorderBrush="{StaticResource PreviewBrushBorder}" BorderThickness="1"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="8,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="145" /><ColumnDefinition Width="80" />
                                            <ColumnDefinition Width="190" /><ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Foreground="{StaticResource PreviewBrushTextMuted}" Text="{Binding TimestampText}" />
                                        <TextBlock Grid.Column="1" Foreground="{StaticResource PreviewBrushWarning}" Text="{Binding Severity}" />
                                        <TextBlock Grid.Column="2" Foreground="{StaticResource PreviewBrushActionBlue}" Text="{Binding EventId}" />
                                        <TextBlock Grid.Column="3" Foreground="{StaticResource PreviewBrushTextSecondary}"
                                                   Text="{Binding Detail}" TextWrapping="Wrap" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </TabItem>
                <TabItem Style="{StaticResource ShellServicesTab}" Header="{localization:PreviewMessage ShellServices_Credits_Tab}">
                    <TextBlock Margin="0,20,0,0" MaxWidth="760" HorizontalAlignment="Left" VerticalAlignment="Top"
                               Foreground="{StaticResource PreviewBrushTextSecondary}" Text="{Binding CreditsText}" TextWrapping="Wrap" />
                </TabItem>
                <TabItem Style="{StaticResource ShellServicesTab}" Header="{localization:PreviewMessage ShellServices_Licenses_Tab}">
                    <TextBlock Margin="0,20,0,0" MaxWidth="760" HorizontalAlignment="Left" VerticalAlignment="Top"
                               Foreground="{StaticResource PreviewBrushTextSecondary}" Text="{Binding LicenseText}" TextWrapping="Wrap" />
                </TabItem>
            </TabControl>
        </Grid>
    </Border>
</UserControl>
